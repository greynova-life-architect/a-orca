# a-orca - Environment variables
# Copy to .env and adjust

# HTTP port (default 3457)
PORT=3457

# Path to cursor-agent executable or PowerShell wrapper
# Windows: e.g. C:\Users\You\.cursor\bin\cursor-agent.ps1
# Leave empty to auto-detect from PATH or common locations
CURSOR_CLI_PATH=

# API key for Cursor agent (optional)
CURSOR_API_KEY=

# Cursor agent model (optional; default: Auto). Overrides the value set in Settings UI.
# CURSOR_AGENT_MODEL=Auto

# Allowed roots for browse/folder picker (path delimiter-separated)
# Leave empty for default (home, workspace)
BROWSE_ROOTS=
PROJECT_ROOTS=

# Log level: debug, info, warn, error (default: info)
LOG_LEVEL=info

# Feature flags (optional)
# ENABLE_WSL_FALLBACK=0   # Use WSL when cursor-agent not found (0=off, 1=on)
# BROWSE_STRICT_MODE=0    # Strict path validation for browse (0=off, 1=on)
